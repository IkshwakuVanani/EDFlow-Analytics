databases:
  - name: edflow-orchestrator-db
    databaseName: edflow
    user: edflow
    plan: free

services:
  - type: web
    name: edflow-orchestrator-api
    runtime: python
    plan: free
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: edflow-orchestrator-db
          property: connectionString
      - key: EDFLOW_SEED_SAMPLE
        value: "true"
      - key: CORS_ORIGINS
        sync: false
